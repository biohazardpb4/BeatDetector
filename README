BeatDetector
============

BeatDetector is protected by the WTFPL, so have at it

beat detection algorithm tester

required libraries (sudo apt-get install xxx in Ubuntu)
libmpg123-dev
libsndfile-dev
libqt4-dev
